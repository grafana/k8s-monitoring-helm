Grafana {{.Chart.Name}} Helm chart deployed!

This chart will install the following components:
{{- if (index .Values "alloy-operator").deploy }}
* Grafana Alloy Operator
{{- end }}
{{- range $feature := (include "features.list.enabled" .) | fromYamlArray }}
  {{- include (printf "feature.%s.notes.deployments" $feature) (dict "Values" (index $.Values $feature)) | indent 0 }}
{{- end }}
{{- include "collectors.notes.deployments" . }}
{{- include "telemetry-services.notes.deployments" (index .Values "telemetry-services" | default dict) }}

It will:
{{- $isTranslating := false }}
{{- range $feature := (include "features.list.enabled" .) | fromYamlArray }}
  {{- if ne $feature "selfReporting" }}
    {{- $task := include (printf "feature.%s.notes.task" $feature) (dict "Values" (index $.Values $feature)) | trim }}
    {{- $destinations := include (printf "features.%s.destinations" $feature) $ | fromYamlArray }}
    {{- $featureIsTranslating := eq (include (printf "features.%s.destinations.isTranslating" $feature) $) "true" }}
* {{ $task }} and send data to {{ include "english_list" $destinations }}{{ if $featureIsTranslating }}{{ $isTranslating = true }}*{{ end }}.
  {{- end }}
{{- end }}

{{- range $feature := (include "features.list.enabled" .) | fromYamlArray }}
  {{- $collectorName := (index $.Values $feature).collector }}
  {{- $Collector := dict "Namespace" $.Release.Namespace }}
  {{- if $collectorName }}
    {{- $Collector = merge $Collector (dict "ServiceName" (include "collector.alloy.fullname" (merge (dict "collectorName" $collectorName) $))) }}
  {{- end }}
  {{- include (printf "feature.%s.notes.actions" $feature) (merge (dict "Collector" $Collector) $) | indent 0 }}
{{- end }}
{{- if $isTranslating }}

* One or more features will have their data translated to a different storage ecosystem. This may result in an imperfect
  translation of labels and attributes. For more information, consult:
    https://github.com/grafana/k8s-monitoring-helm/tree/main/charts/k8s-monitoring/docs/destinations#ecosystem-translation
{{- end }}
{{- if .Values.global.minScrapeInterval }}
{{- $min := .Values.global.minScrapeInterval }}
{{- $minSec := include "duration_to_seconds" $min | atoi }}
{{- $warnings := list }}
{{- $globalSec := include "duration_to_seconds" .Values.global.scrapeInterval | atoi }}
{{- if lt ($globalSec | int) ($minSec | int) }}
  {{- $warnings = append $warnings (printf "  - global.scrapeInterval: %s → %s" .Values.global.scrapeInterval $min) }}
{{- end }}
{{- if .Values.selfReporting.scrapeInterval }}
  {{- $w := include "check_scrape_interval_floor" (dict "name" "selfReporting.scrapeInterval" "interval" .Values.selfReporting.scrapeInterval "min" $min "minSec" $minSec) }}
  {{- if $w }}{{ $warnings = append $warnings $w }}{{ end }}
{{- end }}
{{- if $warnings }}

WARNING: Minimum scrape interval floor is active ({{ $min }}).
The following configured scrape intervals will be clamped:
{{ join "\n" $warnings }}
{{- end }}

Note: When minScrapeInterval is active, annotation-based autodiscovery scrape intervals
(k8s.grafana.com/metrics.scrapeInterval) will be overridden to {{ $min }} regardless
of their configured value.

Note: Scrape intervals defined in extraConfig or directly in Prometheus Operator CRs
(ServiceMonitor/PodMonitor spec.scrapeInterval) are not affected by this floor.
{{- end }}
