{{ define "agent.config.windows_exporter" }}
// Windows Exporter
discovery.relabel "windows_exporter" {
  targets = discovery.kubernetes.pods.targets
{{- if (index .Values "prometheus-windows-exporter").enabled }}
  rule {
    source_labels = ["__meta_kubernetes_pod_label_app_kubernetes_io_instance"]
    regex = "{{ .Release.Name }}"
    action = "keep"
  }
{{- end }}
  rule {
    source_labels = ["__meta_kubernetes_pod_container_port_name"]
    regex = "metrics"
    action = "keep"
  }
  {{- range $k, $v := (index .Values.metrics "windows-exporter").labelMatchers }}
  rule {
    source_labels = ["__meta_kubernetes_pod_label_{{ $k | replace "." "_" | replace "/" "_" }}"]
    regex = "{{ $v }}"
    action = "keep"
  }
  {{- end }}
  rule {
    source_labels = ["__meta_kubernetes_pod_node_name"]
    action = "replace"
    target_label = "instance"
  }
  rule {
    source_labels = ["__name__"]
    replacement   = {{ required ".Values.cluster.name is a required value. Please set it and try again." .Values.cluster.name | quote }}
    target_label  = "cluster"
  }
}

prometheus.scrape "windows_exporter" {
  job_name   = "integrations/kubernetes/windows-exporter"
  targets  = discovery.relabel.windows_exporter.output
  forward_to = [prometheus.relabel.windows_exporter.receiver]
}

prometheus.relabel "windows_exporter" {
  rule {
    source_labels = ["__name__"]
    regex = "windows_cpu_time_total"
    replacement = "node_cpu_seconds_total"
    target_label = "__name__"
  }
  rule {
    source_labels = ["volume"]
    regex = "windows_logical_disk_size_bytes"
    replacement = "mountpoint"
    target_label = "mountpoint"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_logical_disk_size_bytes"
    replacement = "node_filesystem_size_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_logical_disk_free_bytes"
    replacement = "node_filesystem_avail_bytes"
    target_label = "__name__"
  }
  //Bug in windows_exporter: Missing pod name in metric
  rule {
    source_labels = ["__name__"]
    regex = "windows_container_memory_usage_commit_peak_bytes"
    replacement = "container_memory_rss"
    target_label = "__name__"
  }
  //Bug in windows_exporter: Missing pod name in metric
  rule {
    source_labels = ["__name__"]
    regex = "windows_container_memory_usage_private_working_set_bytes"
    replacement = "container_memory_working_set_bytes"
    target_label = "__name__"
  }
  //Bug in windows_exporter: Missing pod name in metric
  rule {
    source_labels = ["__name__"]
    regex = "windows_container_cpu_usage_seconds_total"
    replacement = "container_cpu_usage_seconds_total"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_cs_hostname"
    replacement = "kubelet_node_name"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_memory_available_bytes"
    replacement = "node_memory_MemFree_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_os_physical_memory_free_bytes"
    replacement = "node_memory_MemAvailable_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_cs_physical_memory_bytes"
    replacement = "node_memory_MemTotal_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_memory_cache_bytes"
    replacement = "node_memory_Cached_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_memory_commit_limit"
    replacement = "node_memory_CommitLimit_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_memory_committed_bytes"
    replacement = "node_memory_Committed_AS_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_os_paging_free_bytes"
    replacement = "node_memory_SwapFree_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_os_paging_limit_bytes"
    replacement = "node_memory_SwapTotal_bytes"
    target_label = "__name__"
  }
  rule {
    source_labels = ["__name__"]
    regex = "windows_memory_modified_page_list_bytes"
    replacement = "node_memory_Dirty_bytes"
    target_label = "__name__"
  }
{{- if (index .Values.metrics "windows-exporter").allowList }}
  rule {
    source_labels = ["__name__"]
    regex = "up|{{ join "|" (index .Values.metrics "windows-exporter").allowList }}"
    action = "keep"
  }
{{- end }}
  forward_to = [prometheus.remote_write.grafana_cloud_prometheus.receiver]
}
{{ end }}
