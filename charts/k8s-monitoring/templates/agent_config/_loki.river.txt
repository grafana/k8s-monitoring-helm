{{ define "agent.config.loki" }}
// Grafana Cloud Loki
remote.kubernetes.secret "loki_credentials" {
  name = {{ include "kubernetes-monitoring.loki.secretname" . | quote}}
  namespace = {{ .Values.externalServices.loki.secret.namespace | default .Release.Namespace | quote}}
}

{{- with .Values.externalServices.loki }}
loki.write "grafana_cloud_loki" {
  endpoint {
    url = nonsensitive(remote.kubernetes.secret.loki_credentials.data["{{ .hostKey }}"]) + "{{ .writeEndpoint }}"
{{- if .tenantId }}
    tenant_id = remote.kubernetes.secret.loki_credentials.data["{{ .tenantIdKey }}"]
{{- end }}
{{- if .proxyURL }}
    proxy_url = {{ .proxyURL | quote }}
{{- end }}
{{ if .basicAuth }}
    basic_auth {
      {{ if .basicAuth.username }}username = nonsensitive(remote.kubernetes.secret.loki_credentials.data["{{ .basicAuth.usernameKey }}"]){{ end }}
      {{ if .basicAuth.password }}password = remote.kubernetes.secret.loki_credentials.data["{{ .basicAuth.passwordKey }}"]{{ end }}
    }
{{- end }}
  }
  external_labels = {
    {{- range $k, $v := .externalLabels }}
    {{ $k }} = {{ $v | quote }},
    {{- end }}
{{- end }}
    cluster = {{ required ".Values.cluster.name is a required value. Please set it and try again." .Values.cluster.name | quote }},
  }
}
{{ end }}
