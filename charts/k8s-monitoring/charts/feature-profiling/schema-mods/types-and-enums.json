{
  "properties": {
    "ebpf": {
      "properties": {
        "demangle": { "enum": ["none", "simplified", "templates", "full"]},
        "targetingScheme": { "enum": ["all", "annotation"] }
      }
    },
    "java": {
      "properties": {
        "targetingScheme": { "enum": ["all", "annotation"] }
      }
    }
  }
}
