# yamllint disable rule:document-start rule:line-length rule:trailing-spaces
suite: Feature - Profiling - pprof Profiling
templates:
  - test/configmap.yaml
tests:
  - it: should build the pprof profiling configuration
    set:
      testing: {enabled: true}
      ebpf:
        enabled: false
      java:
        enabled: false
      pprof:
        enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
  - it: should be able to filter by label and annotation
    set:
      testing: {enabled: true}
      ebpf:
        enabled: false
      java:
        enabled: false
      pprof:
        enabled: true
        labelSelectors:
          app.kubernetes.io/name: secret-program
          colors: [blue, green]
        annotationSelectors:
          region: central
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
  - it: allows you to remove the bearer token
    set:
      testing: {enabled: true}
      ebpf:
        enabled: false
      java:
        enabled: false
      pprof:
        enabled: true
        bearerTokenFile: ""
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
