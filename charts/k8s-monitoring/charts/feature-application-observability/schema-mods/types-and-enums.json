{
  "properties": {
    "connectors": {
      "properties": {
        "spanMetrics": {
          "properties": {
            "histogram": {"properties": {"type": {"enum": ["explicit", "exponential"]}}},
            "metricsExpiration": {"type": ["string", "null"]},
            "seriesExpiration": {"type": ["string", "null"]}
          }
        }
      }
    },
    "processors": {
      "properties": {
        "filter": {
          "properties": {
            "errorMode": {"enum": ["ignore", "silent", "propagate"]}
          }
        },
        "transform": {
          "properties": {
            "errorMode": {"enum": ["ignore", "silent", "propagate"]}
          }
        }
      }
    }
  }
}
