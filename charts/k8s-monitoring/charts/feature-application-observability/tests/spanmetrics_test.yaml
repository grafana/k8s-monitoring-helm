# yamllint disable rule:document-start rule:line-length rule:trailing-spaces
suite: Feature - Application Observability - Spanmetrics connector
templates:
  - test/configmap.yaml
tests:
  - it: creates the pipeline with the spanmetrics connector
    set:
      testing: {enabled: true}
      connectors:
        spanMetrics:
          enabled: true
          dimensions:
            - name: "http.status_code"
            - name: "http.method"
              default: "GET"
      receivers:
        otlp:
          grpc:
            enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
