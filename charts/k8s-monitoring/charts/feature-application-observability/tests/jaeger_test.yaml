# yamllint disable rule:document-start rule:line-length rule:trailing-spaces rule:empty-lines
suite: Feature - Application Observability - Jaeger receiver
templates:
  - test/configmap.yaml
tests:
  - it: should allow you to enable just the jaeger grpc receiver
    set:
      testing: {enabled: true}
      receivers:
        jaeger:
          grpc:
            enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]

  - it: should allow you to enable just the jaeger thrift binary receiver
    set:
      testing: {enabled: true}
      receivers:
        jaeger:
          thriftBinary:
            enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]

  - it: should allow you to enable just the jaeger thrift compact receiver
    set:
      testing: {enabled: true}
      receivers:
        jaeger:
          thriftCompact:
            enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]

  - it: should allow you to enable just the jaeger thrift http receiver
    set:
      testing: {enabled: true}
      receivers:
        jaeger:
          thriftHttp:
            enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
