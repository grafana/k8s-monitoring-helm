# yamllint disable rule:document-start rule:line-length rule:trailing-spaces
suite: Feature - Application Observability - Default values
templates:
  - test/configmap.yaml
tests:
  - it: creates the default pipeline
    set:
      testing: {enabled: true}
      receivers:
        otlp:
          grpc:
            enabled: true
          http:
            enabled: true
        jaeger:
          grpc:
            enabled: true
          thriftBinary:
            enabled: true
          thriftCompact:
            enabled: true
          thriftHttp:
            enabled: true
        zipkin:
          enabled: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
