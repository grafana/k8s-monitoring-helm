---
global:
  # -- Override the namespace for namespaced resources created by this chart.
  # @section -- Global Settings
  namespaceOverride: ""

# -- Port number on which the server listens for new connections.
# @section -- Listener Configuration
port: 8080

# -- Rule blocks to be added to the prometheus.relabel component for received metrics.
# These relabeling rules are applied to metrics received by this feature.
# ([docs](https://grafana.com/docs/alloy/latest/reference/components/prometheus/prometheus.relabel/#rule))
# @section -- Metric Processing
metricProcessingRules: ""
