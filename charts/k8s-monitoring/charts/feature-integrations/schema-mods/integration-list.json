{
  "definitions": {
    "alloy-integration": {
      "properties": {"type": {"type": "string", "const": "alloy"}}
    },
    "cert-manager-integration": {
      "properties": {"type": {"type": "string", "const": "cert-manager"}}
    },
    "dcgm-exporter-integration": {
      "properties": {"type": {"type": "string", "const": "dcgm-exporter"}}
    },
    "etcd-integration": {
      "properties": {"type": {"type": "string", "const": "etcd"}}
    },
    "grafana-integration": {
      "properties": {"type": {"type": "string", "const": "grafana"}}
    },
    "istio-integration": {
      "properties": {"type": {"type": "string", "const": "istio"}}
    },
    "loki-integration": {
      "properties": {"type": {"type": "string", "const": "loki"}}
    },
    "mimir-integration": {
      "properties": {"type": {"type": "string", "const": "mimir"}}
    },
    "mysql-integration": {
      "properties": {"type": {"type": "string", "const": "mysql"}}
    },
    "postgresql-integration": {
      "properties": {"type": {"type": "string", "const": "postgresql"}}
    },
    "tempo-integration": {
      "properties": {"type": {"type": "string", "const": "tempo"}}
    },
    "integration-list": {
      "type": "array",
      "items": {
        "anyOf": [
          { "$ref": "#/definitions/alloy-integration"},
          { "$ref": "#/definitions/cert-manager-integration"},
          { "$ref": "#/definitions/dcgm-exporter-integration"},
          { "$ref": "#/definitions/etcd-integration"},
          { "$ref": "#/definitions/grafana-integration"},
          { "$ref": "#/definitions/istio-integration"},
          { "$ref": "#/definitions/loki-integration"},
          { "$ref": "#/definitions/mimir-integration"},
          { "$ref": "#/definitions/mysql-integration"},
          { "$ref": "#/definitions/postgresql-integration"},
          { "$ref": "#/definitions/tempo-integration"}
        ]
      }
    }
  }
}
