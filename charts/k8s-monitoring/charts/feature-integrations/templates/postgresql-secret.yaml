{{- range $instance := .Values.postgresql.instances }}
  {{- $instance = merge $instance (dict "type" "integration.postgresql") }}
  {{- if eq (include "secrets.shouldCreateKubernetesSecret" $instance ) "true" }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "secrets.kubernetesSecretName" (dict "Values" $.Values "Chart" $.Chart "Release" $.Release "object" $instance "name" $instance.name) | quote }}
  namespace: {{ include "secrets.kubernetesSecretNamespace" (dict "Values" $.Values "Release" $.Release "object" $instance) | quote }}
stringData:
{{- $secrets := include "secrets.list.integration.postgresql" $instance | fromYamlArray }}
{{- range $secret := $secrets }}
  {{- $value := include "secrets.getSecretValue" (dict "object" $instance "key" $secret) -}}
  {{- if $value }}
  {{ include "secrets.getSecretKey" (dict "object" $instance "key" $secret) }}: {{ $value | quote }}
  {{- end }}
{{- end }}
  {{- end -}}
{{- end -}}
