{{- if .Values.testing }}
{{- if .Values.testing.enabled }}
{{- $context := dict "Values" .Values "Files" .Files "Release" .Release }}
{{- include "feature.cloudwatchMetrics.validate" $context }}
{{- $alloyConfig := include "feature.cloudwatchMetrics.module" $context }}
{{- $alloyConfig = regexReplaceAll `[ \t]+(\r?\n)` $alloyConfig "\n" }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-test-config
  namespace: {{ include "helper.namespace" . }}
data:
  module.alloy: |-
    {{- $alloyConfig | trim | nindent 4 }}
{{- end }}
{{- end }}
