# yamllint disable rule:document-start rule:line-length rule:trailing-spaces
suite: Feature - Cluster Metrics - Proxy-based Node Address Discovery
templates:
  - test/configmap.yaml
tests:
  - it: should be able to find the Kubelet and cAdvisor via the API server proxy
    set:
      testing: {enabled: true}
      kubelet:
        nodeAddressFormat: proxy
      kubeletResource:
        nodeAddressFormat: proxy
      kubeletProbes:
        enabled: true
        nodeAddressFormat: proxy
      cadvisor:
        nodeAddressFormat: proxy
      kube-state-metrics:
        enabled: false
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
