# yamllint disable rule:document-start rule:line-length rule:trailing-spaces
suite: Feature - Cluster Metrics - Default Values
templates:
  - test/configmap.yaml
tests:
  - it: should render the default configuration
    set:
      testing:
        enabled: true
        telemetryServices:
          kube-state-metrics:
            deploy: true
    asserts:
      - isKind:
          of: ConfigMap
      - matchSnapshot:
          path: data["module.alloy"]
