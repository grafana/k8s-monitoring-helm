---
cluster:
  name: loki-batch-config-test

destinations:
  - name: lokiWithBatchConfig
    type: loki
    url: http://loki.loki.svc:3100/loki/api/v1/push
    batchSize: "2MiB"
    batchWait: "2s"
    tenantId: "1"
    auth:
      type: basic
      username: loki
      password: lokipassword

podLogs:
  enabled: true

alloy-logs:
  enabled: true 