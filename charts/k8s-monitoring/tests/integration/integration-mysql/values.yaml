---
cluster:
  name: mysql-integration-test

destinations:
  - name: localPrometheus
    type: prometheus
    url: http://prometheus-server.prometheus.svc:9090/api/v1/write

integrations:
  mysql:
    instances:
      - name: test-database
        exporter:
          dataSource:
            host: test-database.mysql.svc
            username:

alloy-metrics:
  enabled: true
