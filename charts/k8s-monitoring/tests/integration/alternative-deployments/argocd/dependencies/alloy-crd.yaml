---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy-crd
  namespace: kube-system
spec:
  interval: 1m
  chart:
    spec:
      chart: alloy-crd
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: flux-system
      interval: 1m
