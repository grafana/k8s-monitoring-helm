---
cluster:
  name: pod-logs-via-volumes-test

destinations:
  - name: localLoki
    type: loki
    url: http://loki.loki.svc:3100/loki/api/v1/push
    tenantId: "1"
    auth:
      type: basic
      username: loki
      password: lokipassword

podLogs:
  enabled: true
  volumeGathering:
    enabled: true

alloy-logs:
  enabled: true
