---
tests:
  - env:
      LOKI_URL: http://loki.loki.svc:3100/loki/api/v1/query
      LOKI_TENANTID: 1
      LOKI_USERNAME: loki
      LOKI_PASSWORD: lokipassword
    queries:
      - query: count_over_time({cluster="cluster-events-feature-test", job="integrations/kubernetes/eventhandler"}[1h])
        type: logql
