---
tests:
  - envFrom:
      - secretRef:
          name: grafana-cloud-credentials
      - configMapRef:
          name: test-variables
    queries:
      - query: alloy_build_info{cluster="k8s-monitoring-gc-feature-test", random="$RANDOM_NUMBER"}
        type: promql
