---
cluster:
  name: null-destinations-test

destinations:
  metricsService:
    type: prometheus
    url: http://prometheus.example.com/api/prom
  secondMetricsService:
    type: prometheus
    url: http://prometheus2.example.com/api/prom

hostMetrics:
  enabled: true
  linuxHosts:
    enabled: true
    source: alloy

collectors:
  alloy:
    presets: [linux-host-monitor, daemonset]
