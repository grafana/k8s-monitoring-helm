{
  "definitions": {
    "dataProcessor": {
      "type": "object",
      "anyOf": [
        { "$ref": "#/definitions/custom-dataProcessor"},
        { "$ref": "#/definitions/ec2Enrichment-dataProcessor"},
        { "$ref": "#/definitions/kubernetesEnrichment-dataProcessor"}
      ]
    },
    "dataProcessor-map": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/dataProcessor"
      }
    },
    "custom-dataProcessor": {"properties": {"type": {"type": "string", "const": "custom"}}},
    "ec2Enrichment-dataProcessor": {"properties": {"type": {"type": "string", "const": "ec2Enrichment"}}},
    "kubernetesEnrichment-dataProcessor": {"properties": {"type": {"type": "string", "const": "kubernetesEnrichment"}}}
  }
}
