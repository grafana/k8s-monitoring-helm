---
cluster:
  name: cluster-events-cluster

destinations:
  loki:
    type: loki
    url: http://loki.loki.svc:3100/api/push

clusterEvents:
  enabled: true
  collector: alloy-singleton

collectors:
  alloy-singleton: {}

