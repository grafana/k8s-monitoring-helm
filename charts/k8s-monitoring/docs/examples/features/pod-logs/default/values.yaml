---
cluster:
  name: pod-logs-cluster

destinations:
  loki:
    type: loki
    url: http://loki.loki.svc:3100/api/push

podLogsViaLoki:
  enabled: true

collectors:
  alloy-logs:
    presets: [filesystem-log-reader, daemonset]
