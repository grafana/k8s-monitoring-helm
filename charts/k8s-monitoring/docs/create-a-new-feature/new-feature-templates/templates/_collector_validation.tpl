{{/* Validates that the Alloy instance is appropriate for the given REPLACE_WITH_Feature Name settings */}}
{{/* Inputs: Values (REPLACE_WITH_Feature Name values), Collector (Alloy values), CollectorName (string) */}}
{{- define "feature.REPLACE_WITH_featureName.collector.validate" -}}

{{- end -}}
